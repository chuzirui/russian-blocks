<?xml version="1.0" encoding="utf-8"?>
<!-- Start card: frosted panel + crisp rim (premium, not toy sticker) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/ds_glass_fill" />
    <stroke
        android:width="1dp"
        android:color="@color/ds_glass_stroke" />
    <corners android:radius="28dp" />
</shape>
